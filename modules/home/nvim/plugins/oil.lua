require('oil').setup({
    default_file_explorer = true,
    win_options = {
	  wrap = true,
    },
    delete_to_trash = false,
    skip_confirm_for_simple_edits = true,
    view_options = {
	  show_hidden = true,
    },
})
